# Simple integration of Spring Boot and SmallRye Config

```
$ gradle runBootJar
```
or if you don't have Gradle installed
```
$ ./gradlew runBootJar
```

Branch `main` show the bug in `SmallRye Common Classloader`.
Branch `fix` show the fixed version.
